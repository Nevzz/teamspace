# URL of your deployed Google Apps Script Web App (ends in /exec).
# Leave unset (or keep the placeholder) to run TeamSpace in local demo mode with sample data.
VITE_GOOGLE_APPS_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
